# Local development: `pnpm env:local` writes these to apps/web/.env.local.
# Production: set the same names in the deployment environment.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
