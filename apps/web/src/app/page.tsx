export default function HomePage() {
  return (
    <main className="shell">
      <section className="intro" aria-labelledby="page-title">
        <span className="mark" aria-hidden="true">
          M
        </span>
        <p className="eyebrow">Operator platform</p>
        <h1 id="page-title">MARKD</h1>
        <p className="lede">
          The foundation for a provenance-aware, bilateral Work Graph.
        </p>
        <p className="status">Repository bootstrap active</p>
      </section>
    </main>
  );
}
