## Problem
FLO-140 is merged on main. The production browser gate now accepts the signed WhatsApp webhook, but its E2E processor subprocess fails with `API database is not configured`. The API server receives the local Supabase DB URL only through its own CI shell export; the fixture subprocess starts later without that export.

## Scope and implementation
Extend the GitHub-Actions-only private `apps/api/.env` generated by `pnpm env:local` to include the local Supabase DB URL from `supabase status`, alongside the random webhook secret. Accept only PostgreSQL URLs on exact loopback hosts. Keep exclusive file creation, mode 0600, and Git ignore protection. The API and test worker read the same settings file.

## Architecture and migrations
No domain, auth/RLS, webhook verification, schema, contract, or migration change. No workflow-file or OAuth-scope change.

## Verification
- Focused env-local unit tests: 6 passed
- TypeScript typecheck and Prettier: passed
- Independent GPT-6 Sol security/diff review: no blocker
- Full PR CI and final merged-main production browser, operator stability, and participant preview gates remain to pass.

## Risk and follow-up
The file is CI-only and does not print credentials. FLO-140 remains open until the production gate passes on main.
